[package]
name = "ewm-compositor"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0"
description = "Wayland compositor with Emacs integration"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
